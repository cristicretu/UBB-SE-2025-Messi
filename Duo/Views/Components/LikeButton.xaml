<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Duo.Views.Components.LikeButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Duo.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <SolidColorBrush x:Key="HeartDefaultBrush" Color="Black"/>
            <SolidColorBrush x:Key="HeartHoverBrush" Color="Red"/>
            <SolidColorBrush x:Key="HeartFilledBrush" Color="Red"/>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Button x:Name="likeButton"
                Grid.Column="0"
                Background="Transparent"
                BorderThickness="0"
                Padding="8,4"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Click="LikeButton_Click">
            <!-- Prevent background/foreground changes -->
            <Button.Resources>
                <ResourceDictionary>
                    <Style TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                    </Style>
                </ResourceDictionary>
            </Button.Resources>
            
            <FontIcon x:Name="heartIcon"
                     Glyph="&#xEB51;"
                     Foreground="{StaticResource HeartDefaultBrush}"
                     FontSize="20"/>
        </Button>
        
        <TextBlock Text="{x:Bind LikeCount, Mode=OneWay}"
                   Grid.Column="1"
                   FontSize="16"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   VerticalAlignment="Center"
                   Margin="4,0,0,0" />
    </Grid>
</UserControl>