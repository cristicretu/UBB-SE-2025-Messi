<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Duo.Views.Components.PostComponent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Duo.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
            BorderThickness="1" 
            CornerRadius="8" 
            Padding="16" 
            Margin="0,0,0,12">
        <StackPanel>
            <!-- Debug Border with all bound properties -->
            <Border Background="Yellow" Padding="8" BorderBrush="Orange" BorderThickness="1" CornerRadius="4" Margin="0,0,0,8">
                <StackPanel>
                    <TextBlock Text="Component Debug:" FontWeight="Bold" Margin="0,0,0,4"/>
                    <TextBlock>
                        <Run Text="Username:" FontWeight="SemiBold"/>
                        <Run Text="{x:Bind Username}"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Title:" FontWeight="SemiBold"/>
                        <Run Text="{x:Bind Title}"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Date:" FontWeight="SemiBold"/>
                        <Run Text="{x:Bind FormattedDate}"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Content:" FontWeight="SemiBold"/>
                        <Run Text="{x:Bind TruncatedContent}"/>
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Likes:" FontWeight="SemiBold"/>
                        <Run Text="{x:Bind LikeCount}"/>
                    </TextBlock>
                </StackPanel>
            </Border>
            
            <Grid Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="{x:Bind Username}" 
                           Grid.Column="0"
                           FontWeight="SemiBold" 
                           Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>

                <TextBlock Text="{x:Bind FormattedDate}" 
                           Grid.Column="1"
                           FontSize="12" 
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                           HorizontalAlignment="Right"/>
            </Grid>

            <TextBlock Text="{x:Bind Title}" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       TextWrapping="Wrap"
                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                       Margin="0,0,0,8"/>
            
            <!-- Hashtags as pills -->
            <ItemsControl ItemsSource="{x:Bind Hashtags}"
                          Margin="0,0,0,8">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                                BorderThickness="0"
                                CornerRadius="16"
                                Padding="8,4"
                                Margin="0,0,6,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="#" 
                                           FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{Binding}" 
                                           FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
                       
            <TextBlock Text="{x:Bind TruncatedContent}" 
                       TextWrapping="Wrap" 
                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                       Margin="0,0,0,8"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Button Content="Like" 
                        Grid.Column="0"
                        Style="{ThemeResource AccentButtonStyle}"
                        CornerRadius="8"
                        Padding="12,4"/>
                
                <TextBlock Text="{x:Bind LikeCount}" 
                           Grid.Column="1"
                           FontSize="12" 
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                           VerticalAlignment="Center"
                           Margin="8,0,0,0"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl> 